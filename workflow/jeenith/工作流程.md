# 志极 Jeenith — 工作流程（提炼存档）

> **来源**：`D:\Code\Project\Qore\Jeenith\docs\工作流程.md`（v1.0.0 / 2026-07-16）
> **提炼存档**：2026-07-16 → `D:\Code\.Rules\workflow\jeenith\`
> **定位**：Jeenith 项目实战工作手册样本，供 `.Rules` 规则体系跨项目参考。提炼自原项目 docs，剥离项目内相对链接，保留全部方法论与实战细节，尾部附「可复用经验提炼」。
>
> **原项目配套文档**（见原仓库，非本存档）：
> - 构建管道细节：`docs/FLUTTER_APK_BUILD_PIPELINE.md`
> - 项目定制规则：`CLAUDE.md` / `AGENTS.md`（项目根目录）
> - 身份信息规范：`D:\Code\.Rules\OrganizationAndUser.md`（跨项目 SSOT，存于规则库）
>
> **目标**：把"项目结构 → 日常开发 → 构建 → 归档 → 发布 → 文档"的全流程沉淀成一份可直接消费的工作手册，供人类开发者与 AI 智能体共同遵循。

---

## 目录

- [0. 项目概览](#0-项目概览)
- [1. 顶层目录结构与含义](#1-顶层目录结构与含义)
- [2. mobile/ Flutter 项目结构详解](#2-mobile-flutter-项目结构详解)
- [3. 产物命名规范](#3-产物命名规范)
- [4. 版本号管理](#4-版本号管理)
- [5. 构建流程](#5-构建流程)
- [6. 归档与历史记录](#6-归档与历史记录)
- [7. 发布流程](#7-发布流程)
- [8. 文档体系](#8-文档体系)
- [9. Git 协作规范](#9-git-协作规范)
- [10. 常用命令速查](#10-常用命令速查)
- [附录 A：版本演进里程碑](#附录-a版本演进里程碑)
- [附录 B：可复用经验提炼](#附录-b可复用经验提炼)

---

## 0. 项目概览

| 属性 | 值 |
|------|-----|
| 中文名 / 英文名 | 志极 / Jeenith |
| 项目类型 | 移动 App（Flutter，Android + Windows 桌面）|
| 所属组织 | Qore（叩心）|
| 应用包名 | `com.qore.jeenith` |
| 当前版本 | 见原项目 `CLAUDE.md`（截至 2026-07-16：v2.4.0+24）|
| 项目位置 | `D:\Code\Project\Qore\Jeenith` |
| 开源协议 | MIT（详见原项目根目录 `LICENSE`）|

**定位**：叩问本心的卜算合集——小六壬、周易、梅花易数、掷筊、紫微斗数、奇门遁甲、抽签、测字、大六壬、风水罗盘 + 使用手册。一个 APP 首页选术，可扩展卜算框架（加新术 = 新建 feature 目录 + 注册一行）。

---

## 1. 顶层目录结构与含义

```
Jeenith/                          # 仓库根目录（solution root）
├── mobile/                       # Flutter 项目主目录（见 §2）
├── backend/                      # 后端占位（当前为空，仅 README）
├── design/                       # 设计稿占位
├── docs/                         # 项目文档（见 §8）
├── tools/                        # 工具脚本占位
├── builds/                       # 持久化产物归档（见 §6）
├── log/                          # 开发日志（update / crush，见 §8.4）
├── ScreenShot/                   # 项目截图素材
├── AGENTS.md                     # AI 智能体规则（与 CLAUDE.md 同步）
├── CLAUDE.md                     # Claude Code 项目规则
├── LICENSE                       # MIT 协议
├── README.md                     # 项目说明
├── build_history.json            # 项目内历史副本（与 builds/build_history.json 同步）
└── .gitignore
```

| 目录/文件 | 含义 | 是否入 Git |
|-----------|------|-----------|
| `mobile/` | Flutter 主项目，所有 Dart 代码 + 各平台原生壳 | ✅ |
| `backend/` | 后端代码占位，Jeenith 当前为纯客户端 App | ✅（仅 README） |
| `design/` | 设计稿占位（Figma 链接、PSD 等） | ✅（仅 README） |
| `docs/` | 项目全生命周期文档：立项 / 需求 / 设计 / 开发 / 测试 / 部署 / 收尾 / 用户文档 | ✅ |
| `tools/` | 工具脚本占位（如批量处理、数据迁移） | ✅（仅 README） |
| `builds/` | **产物归档区**：APK / Windows zip / release_notes / 历史记录 | ❌（产物用 NAS/云盘备份） |
| `log/update/` | 开发更新日志（按"类型_日期_序号.md"命名） | ✅ |
| `log/crush/` | 崩溃日志（按"crush_日期_时间.md"命名） | ✅ |
| `ScreenShot/` | 项目截图，README 引用素材 | ✅ |
| `AGENTS.md` / `CLAUDE.md` | AI 协作规则，与代码同步演进 | ✅ |

---

## 2. mobile/ Flutter 项目结构详解

```
mobile/
├── lib/                          # Dart 源码（核心）
│   ├── main.dart                 # 入口（async + ProviderScope + 桌面窗口初始化）
│   ├── app.dart                  # JeenithApp（MaterialApp.router + Starfield + 主题）
│   ├── core/                     # 核心能力层
│   │   ├── branding.dart         # 品牌常量（组织名、版权行）
│   │   ├── calendar/             # 农历/节气（lunar_sar 服务）
│   │   ├── config/               # AppConfig（动效开关、主题）、平台信息
│   │   ├── divination/           # 卜算框架：DivinationTech 抽象 + Registry
│   │   ├── history/              # 历史记录存储 + 导出
│   │   ├── rng/                  # 真随机引擎（系统熵 + 触摸 + random.org + SHA256）
│   │   ├── theme/                # AppTheme + animations.dart（动效时长常量）
│   │   └── animation/            # 动效系统
│   │       ├── painters/         # 复用 CustomPainter
│   │       ├── particles/        # 粒子系统
│   │       ├── reveal/           # 结果揭示动画（墨晕 + 打字机）
│   │       ├── ritual/           # 入场仪式（12 个卜算术各一）
│   │       └── transitions/      # 路由转场
│   ├── data/                     # 静态数据层
│   │   └── yijing/               # 64卦 + 八卦 + 卦辞爻辞（周易/梅花共用）
│   ├── features/                 # 业务特性层（一术一目录）
│   │   ├── home/                 # 首页（选术 + 仪式路由分发）
│   │   ├── xiaoliuren/           # 小六壬
│   │   ├── zhouyi/               # 周易
│   │   ├── meihua/               # 梅花易数
│   │   ├── jiaobei/              # 掷筊
│   │   ├── ziwei/                # 紫微斗数
│   │   ├── qimen/                # 奇门遁甲
│   │   ├── chouqian/             # 抽签
│   │   ├── cezi/                 # 测字
│   │   ├── daliuren/             # 大六壬
│   │   ├── luopan/               # 风水罗盘（含传感器）
│   │   ├── bazi/                 # 八字推演
│   │   ├── name_test/            # 测名字
│   │   ├── manual/               # 使用手册
│   │   ├── settings/             # 设置页
│   │   └── history/              # 历史记录页
│   ├── providers/                # Riverpod providers barrel
│   ├── router/                   # go_router 路由表（含仪式路由）
│   └── shared/                   # 共享 UI 组件
│       └── widgets/              # GoldButton / DarkButton / Starfield / ...
│
├── android/                      # Android 原生壳
│   └── app/src/main/kotlin/com/qore/jeenith/MainActivity.kt
├── ios/                          # iOS 原生壳（未发布）
├── windows/                      # Windows 桌面壳
│   └── runner/Runner.rc          # ★ exe 版本信息 / 版权 / 图标资源
├── macos/                        # macOS 原生壳（未发布）
├── linux/                        # Linux 原生壳（未发布）
├── web/                          # Web 壳（未发布）
│
├── assets/                       # 静态资源
│   ├── icons/                    # SVG 图标
│   └── yijing/                   # 64 卦 JSON
├── ico/                          # App 图标设计原稿（PSD/PNG）
├── icon_design/                  # 图标 SVG 源文件
│
├── scripts/                      # 构建脚本（见 §5）
│   ├── build_apk.ps1             # 主构建脚本（APK 全自动）
│   └── archive_history.py        # 构建历史追加脚本
│
├── test/                         # 单元测试
├── pubspec.yaml                  # 依赖 + 版本号来源
├── pubspec.lock                  # 依赖锁定
├── analysis_options.yaml         # flutter analyze 规则
├── build_history.json            # 项目内历史副本
└── build/                        # Flutter 原始构建产物（gitignore）
```

### 2.1 features/ 一术一目录的标准结构

每个卜算术遵循同一布局：

```
features/<tech>/
├── <tech>_tech.dart              # 实现 DivinationTech 接口 + 注册到 Registry
├── algorithm/
│   └── divine.dart               # 起卦/排盘核心算法
├── ui/
│   └── <tech>_page.dart          # 主页面（用户交互）
└── data/  state/  sensor/        # 可选：静态数据 / 状态 / 传感器
```

**加新卜算术 = 4 步**：

1. 新建 `features/xxx/` 目录
2. 实现 `DivinationTech` 接口（`xxx_tech.dart`）
3. 在 `divination_registry.dart` 注册一行
4. 新建入场仪式 `core/animation/ritual/xxx_ritual.dart` 并在 `home_page.dart` 的 `_ritualTechs` 集合 + `app_router.dart` 接入

### 2.2 core/animation/ritual/ 入场仪式体系

| 文件 | 卜算术 | 时长 |
|------|--------|------|
| `xiaoliuren_ritual.dart` | 小六壬 | （在 features/xiaoliuren/ui 下）|
| `zhouyi_ritual.dart` | 周易 | 见 `core/theme/animations.dart` 的 AppAnimations |
| `meihua_ritual.dart` | 梅花易数 | 同上 |
| `jiaobei_ritual.dart` | 掷筊 | 同上 |
| `ziwei_ritual.dart` | 紫微斗数 | 同上 |
| `qimen_ritual.dart` | 奇门遁甲 | 同上 |
| `chouqian_ritual.dart` | 抽签 | 同上 |
| `cezi_ritual.dart` | 测字 | 同上 |
| `daliuren_ritual.dart` | 大六壬 | 同上 |
| `luopan_ritual.dart` | 风水罗盘 | 同上 |
| `bazi_ritual.dart` | 八字推演 | 同上 |
| `name_test_ritual.dart` | 测名字 | 同上 |

所有仪式继承 `RitualAnimation` / `RitualAnimationState<T>`，通过 `CustomPaint + AnimationController` 实现，播完调用 `complete()`，用 `ritualScaffold()` 包装提供"跳过"按钮。**CustomPainter 实现必须显式 dispose TextPainter**（项目硬性规则）。

---

## 3. 产物命名规范

### 3.1 通用格式

```
{程序名}_{状态}_{版本号}_{构建日期}_{构建序号}.{扩展名}
```

- **程序名**：`Jeenith`（PascalCase）
- **状态**：见 §3.2
- **版本号**：`X.Y.Z`（来自 pubspec.yaml 的 versionName）
- **构建日期**：`YYYYMMDD`（构建机本地时区）
- **构建序号**：`NN`（两位零填充，同日同版本同状态递增）

### 3.2 平台后缀

| 平台 | 命名示例 | 后缀 |
|------|----------|------|
| Android | `Jeenith_release_2.4.0_20260715_01.apk` | `.apk` |
| Windows | `Jeenith_release_2.4.0_20260715_01_windows_x64.zip` | `_windows_x64.zip` |

### 3.3 构建状态（status）

| 英文 | 中文 | 完整性 | 递增规则 |
|------|------|--------|----------|
| `release` | 正式版 | ✅ 完全体 | minor+1，patch 归零 |
| `rc` | 候选版 | ✅ 接近完全体 | patch+1 |
| `fix` | 修复版 | ✅ 完全体 | patch+1 |
| `hotfix` | 紧急修复 | ✅ 完全体 | patch+1 |
| `beta` | 测试版 | ⚠️ 非完全体 | patch+1 |
| `feature` | 功能版 | ⚠️ 非完全体 | patch+1 |
| `alpha` | 内测版 | ❌ 非完全体 | patch+1 |
| `dev` | 开发版 | ❌ 非完全体 | patch+1 |
| `debug` | 调试版 | ❌ 非完全体 | patch+1 |

**最常用**：`fix`（修 bug）、`feature`（新功能阶段性归档）、`release`（正式发布）。

### 3.4 序号规则

| 触发条件 | 序号 |
|----------|------|
| 新版本（X.Y.Z 变化） | 从 `01` |
| 同版本但日期变化 | 从 `01`（不同日期独立计数） |
| 同版本同日期第 N 次构建 | `NN`（递增） |

序号通过扫描 `builds/` 目录中匹配文件计算，**不依赖 `build/`**（`flutter clean` 会清空）。

---

## 4. 版本号管理

### 4.1 pubspec.yaml 格式

```yaml
version: 2.4.0+24
        ^^^^^ ^^
        │     └─ buildNumber（Android versionCode / Windows exe FileVersion 第 4 段）
        └─────── versionName（X.Y.Z 三段式）
```

Flutter 构建时自动把 `+N` 作为 Android `versionCode`、前半段作为 `versionName`。Windows 桌面通过 `Runner.rc` 的 `FILEVERSION` / `PRODUCTVERSION` 同步。

### 4.2 自动递增（build_apk.ps1）

| 场景 | 行为 |
|------|------|
| `-Status release` | minor+1，patch 归零 |
| `-Status fix`（或其他） | patch+1 |
| `-TargetVersion "X.Y.Z"` | 跳转任意版本（major 升级用） |
| **buildNumber** | **永远 +1**（确保系统能识别为升级） |

### 4.3 版本前置策略（关键）

```
正确顺序：更新 pubspec.yaml → flutter build → 重命名归档
                  ↑
              构建前完成
```

**为什么前置？** APK/exe 内部的 `versionCode` 在构建时**烧录**到 `AndroidManifest.xml` / PE 资源段，必须在 `flutter build` 之前修改 `pubspec.yaml`，否则内部版本与文件名不匹配，无法覆盖安装。

**构建失败回滚**：脚本检测 `$LASTEXITCODE -ne 0` 后自动还原 pubspec，不会污染。

### 4.4 Windows exe 属性同步

`mobile/windows/runner/Runner.rc` 第 80–100 行附近维护：

```rc
VALUE "CompanyName", "Qore"
VALUE "FileDescription", "Jeenith"
VALUE "FileVersion", "2.4.0.24"
VALUE "InternalName", "jeenith"
VALUE "LegalCopyright", "Copyright (c) 2026 Qore"   // ★ 无 All rights reserved
VALUE "OriginalFilename", "jeenith.exe"
VALUE "ProductName", "Jeenith"
VALUE "ProductVersion", "2.4.0.24"
```

修改版本时，`FILEVERSION` / `PRODUCTVERSION` 的 4 段数字也要同步更新为 `MAJOR.MINOR.PATCH.BUILDNUMBER`。

---

## 5. 构建流程

### 5.1 单次构建完整流程图

```
开发者执行 pwsh -File scripts/build_apk.ps1 -Status xxx
    ↓
脚本读取 pubspec.yaml 当前版本
    ↓
计算新版本号（自动递增 or -TargetVersion）
    ↓
更新 pubspec.yaml + Runner.rc（前置）
    ↓
flutter build apk --release
    ↓
   ┌─ 成功 → 继续
   └─ 失败 → 回滚 pubspec.yaml → 退出
    ↓
扫描 builds/android/ 计算序号
    ↓
备份原始 app-release.apk 到 build/.../backup/
    ↓
重命名为标准格式
    ↓
复制到 builds/android/ 持久化目录
    ↓
调用 archive_history.py 追加 build_history.json
    ↓
验证 APK 内部 versionCode/versionName 与文件名一致
```

### 5.2 标准命令

```bash
cd mobile

# 1. 依赖与分析（每次构建前必跑）
flutter pub get
flutter analyze      # 期望 0 issue

# 2. Android APK（推荐用脚本，全自动）
pwsh -File scripts/build_apk.ps1 -Status release                  # 正式版
pwsh -File scripts/build_apk.ps1 -Status fix                      # 修复版
pwsh -File scripts/build_apk.ps1 -Status feature                  # 功能版
pwsh -File scripts/build_apk.ps1 -Status release -TargetVersion "3.0.0"  # 跨 major 跳转

# 3. Windows 桌面（手动）
flutter build windows --release
# 产物在 build\windows\x64\runner\Release\，需手动 Compress-Archive 为 zip 归档到 builds/windows/
```

### 5.3 PowerShell 版本要求

| Shell | 是否可用 | 原因 |
|-------|----------|------|
| `pwsh`（PowerShell 7+） | ✅ 推荐 | 完美支持 UTF-8 中文注释 |
| `powershell.exe`（5.x） | ❌ 不可用 | 默认 GBK，中文注释会报错 |

### 5.4 Windows ZIP 手动归档

`build_apk.ps1` 只处理 Android APK，Windows 桌面需手动：

```powershell
$ver = "2.4.0"
$date = "20260715"
$seq = "01"
$src = "mobile\build\windows\x64\runner\Release\*"
$dst = "builds\windows\Jeenith_release_${ver}_${date}_${seq}_windows_x64.zip"
Compress-Archive -Path $src -DestinationPath $dst -Force
```

然后手动追加到两份 `build_history.json`，并按需更新 `release_history.json`。

### 5.5 Windows exe 属性验证

```powershell
(Get-Item "builds\windows\...\Release\jeenith.exe").VersionInfo |
    Select-Object LegalCopyright, ProductVersion, FileVersion
```

确认 `LegalCopyright` = `Copyright (c) 2026 Qore`（无 "All rights reserved"），`ProductVersion` 与文件名一致。

---

## 6. 归档与历史记录

### 6.1 归档目录结构

```
builds/
├── android/                      # APK 归档（脚本自动写入）
│   ├── Jeenith_release_1.0.0_20260711_01.apk
│   ├── Jeenith_fix_1.1.2_20260712_01.apk
│   └── Jeenith_release_2.4.0_20260715_01.apk
├── windows/                      # Windows zip 归档（手动写入）
│   └── Jeenith_release_2.4.0_20260715_01_windows_x64.zip
├── release_notes/                # 各版本发布说明 markdown（见 §7）
│   ├── release_notes_v1.0.0.md
│   └── release_notes_v2.4.0.md
├── build_history.json            # ★ 构建历史主副本
└── release_history.json          # ★ 平台发布记录
```

### 6.2 双份历史策略

| 文件位置 | 角色 | schema 风格 |
|----------|------|-------------|
| `mobile/build_history.json` | 项目内副本（git 跟踪） | version 与 buildNumber 分离 |
| `builds/build_history.json` | 归档区主副本（唯一真相） | version 合并显示（带 buildNumber） |

**两份必须同步**。脚本 `archive_history.py` 在 APK 构建完成时自动追加；Windows zip 需手动同步追加。

### 6.3 build_history.json 必填字段

```json
{
  "version": "2.4.0+24",
  "status": "release",
  "statusLabel": "正式版",
  "date": "20260715",
  "sequence": 1,
  "filename": "Jeenith_release_2.4.0_20260715_01.apk",
  "timestamp": "2026-07-15T23:50:00",
  "fileSize": 57815079,
  "fileSizeFormatted": "55.14 MB",
  "sha256": "BA2CFB282B...",
  "sourcePath": "...",
  "targetPath": "...",
  "platform": "android-arm64,android-arm,android-x64",
  "verificationPassed": true
}
```

**追加规则**：永远追加，不覆盖；SHA256 大写；时间戳 ISO 8601。

### 6.4 SHA256 计算

```powershell
(Get-FileHash "builds\android\xxx.apk" -Algorithm SHA256).Hash
# 返回大写 64 位 hex
```

---

## 7. 发布流程

### 7.1 release_history.json

记录在 GitHub 等平台的 Release 信息，字段对应 GitHub "New release" 表单：

| 字段 | 说明 |
|------|------|
| `tag` | `v<语义版本>`（如 `v2.4.0`）|
| `target` | 目标分支（`main`）|
| `title` | Release 标题 |
| `notesFile` | 指向 `builds/release_notes/release_notes_<tag>.md` |
| `assets[]` | 附件列表（APK + Windows zip），含 size / sha256 / localPath / downloadUrl |
| `label` / `isLatest` / `isPreRelease` | Latest / Pre-release 标记 |
| `status` | `draft`（待发布）/ `published`（已发布）/ `unpublished`（仅本地构建，未上平台）|
| `url` / `publishedAt` | 发布后回填 |

**关键约定**：发布新版本时，前一个版本的 `isLatest` 改为 `false`、`label` 改为 `"None"`，新版本 `isLatest: true, label: "Latest"`。

### 7.2 release_notes/release_notes_vX.Y.Z.md

**为什么用独立 md 而非 JSON 字符串？** JSON 字符串里的 `\n` 是字面转义，从文本编辑器复制粘贴到 GitHub Release notes 框时不会被解释成换行。独立 md 文件是真实换行，全选复制粘贴即可正常 Preview。

### 7.3 标准发布步骤

1. **构建产物**：APK + Windows zip 已归档到 `builds/android/` 与 `builds/windows/`
2. **填写 release_notes**：新建 `builds/release_notes/release_notes_vX.Y.Z.md`，结构参考 v2.4.0（概述 / 各功能分章 / 版本号更新 / 自上一版本以来的提交 / 下载表）
3. **更新 release_history.json**：
   - 旧版本 `isLatest: false`、`label: "None"`
   - 新增版本条目，`isLatest: true`、`label: "Latest"`、`status: "unpublished"`
4. **更新 CLAUDE.md / AGENTS.md**：版本引用同步更新
5. **Git commit + push**：commit message 用 `feat: vX.Y.Z+<N> <一句话摘要>` 开头，body 列变更要点
6. **（可选）平台发布**：在 GitHub New release 中按 release_history.json 字段填表，发布后回填 `url` / `publishedAt` / `downloadUrl`，`status` 改为 `published`

---

## 8. 文档体系

### 8.1 docs/ 全景

```
docs/
├── 项目文档/                     # 全生命周期文档（按 PMBOK 阶段分目录）
│   ├── 00-项目启动 Initiation/
│   ├── 01-需求分析 Requirements/
│   ├── 02-系统设计 Design/
│   ├── 03-开发实施 Development/
│   ├── 04-测试阶段 Testing/
│   ├── 05-部署上线 Deployment/
│   ├── 06-项目收尾 Closure/
│   └── 07-用户文档 User Docs/
├── NEXT_PLAN/                    # 下一阶段规划（按主题/版本）
├── FLUTTER_APK_BUILD_PIPELINE.md # ★ 构建管道规范（详见 §5 配套）
├── PROJECT_SUMMARY.md            # 项目总览
├── 项目命名规范.md
├── 工作流程.md                   # ★ 本存档的源文档
└── a.md                          # 草稿/临时笔记
```

### 8.2 命名规范

- 中文目录用 `XX-中文 English/` 双语（XX 是阶段编号）
- 文档文件名同上：`中文 English.md`
- AI 协作规则文件在仓库根：`CLAUDE.md` / `AGENTS.md`

### 8.3 release_notes 写作规范

参考原项目 `builds/release_notes/release_notes_v2.4.0.md`：

- **概述**：1 段话说明本版本主题
- **分章**：每个功能/修复/变更单独一章，含「背景」「实施」「影响文件」「验证」
- **版本号更新**：单独一章列出 pubspec 版本变化
- **自上一版本以来的提交表**：commit / 类型 / 说明
- **下载表**：平台 / 文件名 / 大小 / SHA-256

### 8.4 log/ 开发日志

**触发**：每次对项目进行修改后。

**位置**：`log/update/`（更新日志）、`log/crush/`（崩溃日志）

**命名**：

- 更新：`{类型}_{YYYYMMDD}_{序号}.md`（类型：feature / fix / refactor / config / docs / style / perf / test）
- 崩溃：`crush_{YYYYMMDD}_{HHMMSS}.md`

**模板**：见原项目 `AGENTS.md` 中的"更新日志规则"小节。

---

## 9. Git 协作规范

### 9.1 分支策略

当前为单分支开发（`main`），所有提交直接 push 到 `main`。如需多人协作或长任务开发，建议：

- `main`：稳定可发布
- `feature/<topic>`：新功能分支，完成后合并
- `fix/<bug-id>`：修复分支

### 9.2 Commit Message 规范

| 类型 | 前缀 | 示例 |
|------|------|------|
| 新功能 | `feat:` | `feat: v2.4.0+24 八字/测名字入场仪式 + ...` |
| Bug 修复 | `fix:` | `fix: 首页右上角按钮间距过近` |
| 重构 | `refactor:` | `refactor: 提取 Starfield 为共享组件` |
| 文档 | `docs:` | `docs: 重写 docs/项目文档/ 39 个文档` |
| 配置 | `config:` | `config: 新增思源宋体字体` |
| 性能 | `perf:` | `perf: 缓存 LunarService 单例` |
| 样式 | `style:` | `style: 统一按钮圆角` |
| 测试 | `test:` | `test: 新增 wuge 单元测试` |

**版本发布 commit**：`feat: vX.Y.Z+<N> <一句话摘要>` 开头，body 列变更要点。

### 9.3 Commit Message 编写要点

- 第一行 ≤ 72 字符，简明扼要
- body 用 `-` 列变更要点（每条一行）
- 多行 message 在 PowerShell 中用多个 `-m` 参数（不能用 bash heredoc）

```powershell
git commit -m "feat: vX.Y.Z+<N> 标题" -m "- 变更 1" -m "- 变更 2" -m "- 变更 3"
```

### 9.4 Push 前检查清单

- [ ] `flutter analyze` 0 issue
- [ ] `flutter pub get` 无报错
- [ ] 产物（如本次有构建）已归档 + 历史已追加
- [ ] CLAUDE.md / AGENTS.md 版本引用已同步
- [ ] release_notes（如本次有发布）已写
- [ ] commit message 符合规范

### 9.5 版权与身份

- **版权行**：`Copyright (c) 2026 Qore`（**无 "All rights reserved"**，按 MIT 协议规范）
- **Dart 文件头**：`// Copyright (c) 2026 Qore`
- **Windows exe 属性**：`Runner.rc` 的 `LegalCopyright` 同上
- 身份信息（组织 / 包名 / 署名）以 `D:\Code\.Rules\OrganizationAndUser.md` 为准，**不硬编码**

---

## 10. 常用命令速查

### 10.1 开发

```bash
cd mobile
flutter pub get
flutter analyze
flutter run -d windows         # Windows 桌面调试
flutter run -d <device-id>     # Android 设备调试
```

### 10.2 构建

```bash
# Android（全自动：版本+构建+重命名+归档+历史）
pwsh -File scripts/build_apk.ps1 -Status release
pwsh -File scripts/build_apk.ps1 -Status fix -TargetVersion "2.5.0"

# Windows（手动 zip）
flutter build windows --release
Compress-Archive -Path "build\windows\x64\runner\Release\*" `
                  -DestinationPath "builds\windows\Jeenith_release_X.Y.Z_YYYYMMDD_01_windows_x64.zip" -Force
```

### 10.3 验证

```powershell
# APK 内部版本
& "$env:ANDROID_HOME\build-tools\34.0.0\aapt.exe" dump badging "builds\android\xxx.apk" |
    Select-String "versionCode|versionName"

# Windows exe 属性
(Get-Item "builds\windows\...\jeenith.exe").VersionInfo |
    Select-Object LegalCopyright, ProductVersion, FileVersion

# SHA256
(Get-FileHash "builds\android\xxx.apk" -Algorithm SHA256).Hash
```

### 10.4 历史

```powershell
# 列出所有历史构建
Get-ChildItem "builds" -Filter "Jeenith_*.apk", "Jeenith_*.zip" -Recurse |
    Sort-Object LastWriteTime -Descending |
    Select-Object Name, @{N='Size(MB)';E={[math]::Round($_.Length/1MB,2)}}, LastWriteTime
```

### 10.5 Git

```bash
git status
git log --oneline -10
git diff --stat
git add -A
git commit -m "feat: vX.Y.Z+<N> ..." -m "- 变更 1" -m "- 变更 2"
git push origin main
```

---

## 附录 A：版本演进里程碑

| 版本 | 里程碑 |
|------|--------|
| v1.0.0 | 首个正式版本（核心 8 术）|
| v1.1.x | 工程迁移至 mobile/ + Windows 桌面 + 真机熵源修复 |
| v1.2.0 | 卦辞爻辞数据（周易/梅花）|
| v1.3.0 / v1.4.0 / v1.7.0 | 紫微 v2 / 奇门 v2 / 大六壬 |
| v1.5.0 | 主题切换 + 结果分享 + 历史导出 |
| v2.0.0 | 体验深化与品牌定调 |
| v2.1.0 / v2.2.0 | 动效体系 Phase 1-6 |
| v2.3.0 | 八字推演 + 测名字 + 紫微盘重构 |
| v2.3.2 | 设置页动画细分开关 + Windows 图标修复 |
| v2.3.3 | MIT LICENSE + 首页按钮间距修复 |
| v2.4.0 | 八字/测名字入场仪式 + 一键获取时间 + 词库扩展 + All rights reserved 全面移除 |

完整提交历史：`git log --oneline`（原项目仓库内）。

---

## 附录 B：可复用经验提炼

> 以下是从 Jeenith 实战中提炼的、**可迁移到其他项目**的非显而易见经验。这些是本存档的核心增量价值——原文档中它们散落在各章节，此处集中显式化。

### B.1 版本号必须前置更新（构建前，非构建后）

**经验**：移动端（Flutter/Android/Windows）的 `versionCode` / exe PE 资源段在 **构建时烧录** 进产物。版本号必须在 `flutter build` **之前**写入 `pubspec.yaml`（及 Windows 的 `Runner.rc`），否则文件名版本与产物内部版本不一致，导致无法覆盖安装。

**配套**：构建脚本检测 `$LASTEXITCODE -ne 0`（失败）后**自动回滚** pubspec.yaml，避免失败时污染版本号。

> 迁移要点：任何"版本号烧录进二进制"的平台都有此约束；CI 中版本更新应作为构建的前置步骤，而非后置重命名步骤。

### B.2 构建序号扫描持久化归档目录，不依赖 build/

**经验**：`flutter clean` 会清空 `build/`，若序号依赖 `build/` 内文件计数会断号。正确做法是扫描**持久化归档目录**（`builds/`）中匹配文件计算序号，保证跨 `clean` 连续。

### B.3 双份历史策略（git 副本 + 归档区主副本）

**经验**：维护两份 `build_history.json`——

- **项目内副本**（git 跟踪）：version 与 buildNumber 分离，便于 diff 与回溯
- **归档区主副本**（唯一真相）：version 合并显示，便于人读

两份 schema 风格可不同以适应各自场景，但**必须同步**。自动构建只追加分副本，手动平台需补同步主副本。

> 迁移要点：当"git 友好的结构"与"人类可读的结构"冲突时，不必强行统一——分两份各取所长，用同步机制保证一致。

### B.4 release_notes 用独立 md，不要塞进 JSON 字符串

**经验**：JSON 字符串里的 `\n` 是**字面转义**，从编辑器复制粘贴到 GitHub Release notes 框时不会被解释为换行，预览全是单行。独立 `.md` 文件是真实换行，全选复制即可正常 Preview。

> 迁移要点：任何需要"复制粘贴到富文本框"的长文本，都应存为独立文件而非 JSON 字符串字段；JSON 只存指向该文件的路径（`notesFile`）。

### B.5 release 的 isLatest 切换约定

**经验**：发布新版本时，**前一个版本**的 `isLatest` 要显式改为 `false`、`label` 改为 `"None"`，新版本置为 `isLatest: true`。状态机：`unpublished`（仅本地）→ `published`（已上平台，回填 url/publishedAt）。

### B.6 Windows 桌面 exe 属性必须与 pubspec 三处同步

**经验**：`mobile/windows/runner/Runner.rc` 维护 exe 右键属性，版本更新时**三处必须同步**：

1. `pubspec.yaml` 的 `version: X.Y.Z+N`
2. `Runner.rc` 的 `FILEVERSION` / `PRODUCTVERSION`（4 段：MAJOR.MINOR.PATCH.BUILDNUMBER）
3. `Runner.rc` 的 `FileVersion` / `ProductVersion` 字符串值

**版权行规范**（MIT）：`Copyright (c) 2026 Qore`——**不加 "All rights reserved"**。MIT 协议本身就保留了权利，重复声明是冗余且不符合协议精神。

### B.7 PowerShell 脚本必须用 pwsh 7+，不可用 5.x

**经验**：`powershell.exe` 5.x 默认编码 GBK，脚本中的**中文注释**会触发解析错误。含中文注释的构建脚本必须用 `pwsh`（PowerShell 7+，默认 UTF-8）。

> 迁移要点：Windows 上任何含非 ASCII 字符（中文注释、中文路径）的脚本，都应显式要求 pwsh 7+，或在脚本头强制设置 UTF-8 编码。

### B.8 插件式可扩展框架："加新 X = 固定 N 步"

**经验**：Jeenith 的卜算框架做到"加新术 = 4 步"（建目录 → 实现接口 → 注册一行 → 接入路由），背后是 `DivinationTech` 抽象 + `Registry` 注册模式的组合。这是高内聚低耦合的落地形态——新增成本固定且极低。

> 迁移要点：当一类业务实体会持续增加（支付方式、通知渠道、卜算术、报表类型…），优先抽象出"接口 + 注册表"框架，把新增成本压到"建目录 + 注册一行"。

### B.9 CustomPainter 必须显式 dispose TextPainter

**经验**：Flutter 的 `CustomPainter` 中使用的 `TextPainter` 不会被自动回收，**必须显式 `dispose()`**，否则内存泄漏。这是 Jeenith 的项目硬性规则。

> 迁移要点：Flutter 动画/自绘场景下，所有实现 `CustomPainter` 的代码都应审查 `TextPainter`（及其他可 dispose 资源）的释放。

### B.10 文档全生命周期分目录（PMBOK 阶段）

**经验**：`docs/项目文档/` 按 PMBOK 7 阶段分目录（00-启动 / 01-需求 / 02-设计 / 03-开发 / 04-测试 / 05-部署 / 06-收尾 / 07-用户文档），每目录用 `XX-中文 English/` 双语命名。这让任何文档都能按"它在生命周期哪个阶段"快速归位，而非按杂乱的日期或主题。

> 迁移要点：项目文档目录按"生命周期阶段"编号组织，比按"文档类型"或"日期"更符合人类对项目的认知模型。

---

*本存档忠实记录 Jeenith 项目工作流程全貌，附录 B 的经验提炼可直接作为其他项目（尤其 Flutter 移动端 + Windows 桌面）的参考清单。*
