# 改动文档 — AI 编码规则体系 v3.4.0

> 生成时间：2026-08-04　|　涉及修改：5 次（功能更新为主）
> 生成依据：`D:\Code\.Rules\common\core\workflow.md` §12（版本号迭代触发，用户确认后生成）

---

## 功能更新

### 1. 组织信息：服务器与域名（SSOT 新增）

**如何使用**：需要服务器 / 备案 / 域名信息时，以 `OrganizationAndUser.md` §三 为准；域名反写规则用于包名冲突判定。

**利弊分析**：
- 利：服务器资产信息集中维护，不再散落各项目；域名与包名前缀的冲突规则明确
- 弊 / 权衡：新增章节导致 OrganizationAndUser.md 后续章节编号顺延（四~六）

**改动细节**：
- `OrganizationAndUser.md` — 新增 §三「服务器与域名」：腾讯云轻量应用服务器 IPv4 `62.234.115.217`、ICP 备案、公安联网备案、个人域名 `heys-snowe.tj.cn`；原 §三~五 顺延为 §四~六

### 2. 组织口号：新增「未雨绸缪，兜底必达」

**如何使用**：组织口号现为双口号——口号·一「叩问本心，不忘初心」（初心之本）与口号·二「未雨绸缪，兜底必达」（做事之道），英文同步补充。

**利弊分析**：
- 利：口号覆盖"本心"与"做事"两个维度，可用于不同语境
- 弊 / 权衡：双口号需在引用处明确区分（口号·一 / 口号·二）

**改动细节**：
- `OrganizationAndUser.md` — 口号表格与 yaml 扩展为双口号结构（中文 + 英文完整/凝练）

### 3. AI 行为约束：两手准备，兜底必达（principles §6.10）

**如何使用**：所有方案默认提供 Plan B；不可逆操作（部署/发布/迁移）先确认回滚方案；备份等兜底资源定期验证可恢复。

**利弊分析**：
- 利：将"两手准备"从口号落地为可执行的行为规则，避免唯一解与"名义兜底"
- 弊 / 权衡：增加方案产出成本；需 AI 判断哪些场景必须给 Plan B（默认所有，除非用户明确只要一种）

**改动细节**：
- `common/core/principles.md` §6.10 — 新增规则（Why + 实践要点 4 条）
- `compact-core.md` §一 — Tier 1 新增第 11 条（始终生效，指向 principles §6.10）

### 4. AI 行为约束：PowerShell 一律用 pwsh（7.6.4 首选，5.1 备用）

**如何使用**：Windows 下执行 PowerShell 命令一律用 `pwsh`（PowerShell 7.6.4），NEVER 用 Windows 自带的 `powershell`（5.1 旧版）。

**利弊分析**：
- 利：pwsh 7.x 默认 UTF-8，中文注释/脚本不乱码；与已有项目经验（jeenith 工作流程 B.7）一致
- 弊 / 权衡：依赖机器已安装 pwsh 7.6.4；5.1 仅作未安装时的备用

**改动细节**：
- `common/core/workflow.md` §3.4 — 工具使用新增 pwsh 规则（源定义 + 原因）
- `compact-core.md` §一 — Tier 1 新增第 12 条（始终生效，指向 workflow §3.4）

---

## Bug 修复

无。

---

## 其他改动

### 版本迭代与同步（v3.3.0 → v3.4.0）

- `main.md` — 版本 v3.3.0 → v3.4.0；变更记录新增 v3.4.0 行
- `compact-core.md` — 尾注版本同步 v3.4.0；「规则文件最后更新时间」表更新（workflow / principles / compact-core → 2026-08-04）
- `README.md` / `CLAUDE.md` / `AGENTS.md` — 版本号同步 v3.4.0
- `~/.deepcode/AGENTS.md` — 全局内嵌版同步：版本核对目标 v3.4.0 + §1 新增两条约束 + 更新时间表

---

*改动文档由 AI 依据 workflow.md §12 生成，供版本追溯与维护者审阅。*
